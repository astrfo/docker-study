{
    "name": "docker-study",
    "dockerFile": "Dockerfile",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ],
            "mounts": [
                "source=.,target=/workspace,type=bind,consistency=cached"
            ],
            "env": {
                "PYTHONPATH": "/home/astrfo"
            }
        }
    }
}